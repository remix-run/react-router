Add `useFrameworkScripts` to expose Framework Mode script and link descriptors for custom rendering

This lets apps render the framework-managed `<script>` and `<link>` tags themselves and apply additional HTML attributes directly at the render site.
