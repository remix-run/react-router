Remove `createStaticHandler`'s `mapRouteProperties` option since the router is no longer React-agnostic and mapping logic now lives internally

- Also removed from `UNSAFE_createRouter`'s initialization options
