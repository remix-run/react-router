Fix hydration of streamed `Suspense` boundaries in development by wrapping the dev-only critical CSS reset in `React.startTransition`. Previously the reset triggered a synchronous update while boundaries below were still dehydrated, causing React 18 to discard their server HTML and re-render them on the client (`"This Suspense boundary received an update before it finished hydrating"`)
