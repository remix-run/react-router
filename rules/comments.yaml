id: comments
language: "ts"
rule:
  kind: comment
  pattern: $COMMENT
  regex: "@remix-run\\/"
  not:
    regex: "@remix-run\/router"
transform:
  COMMENT:
    replace:
      by: "@react-router/"
      replace: "@remix-run\\/"
      source: $COMMENT
fix: $COMMENT
