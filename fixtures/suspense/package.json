{
  "name": "suspense",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "prettier": "^1.18.2",
    "react": "^0.0.0-experimental-f6b8d31a7",
    "react-dom": "^0.0.0-experimental-f6b8d31a7",
    "react-scripts": "3.2.0"
  },
  "scripts": {
    "prestart": "cp -r ../../packages/* ./node_modules/",
    "prebuild": "cp -r ../../packages/* ./node_modules/",
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
