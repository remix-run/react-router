/** @jsx React.DOM */

var React = require('react');

var Inbox = React.createClass({

  render: function () {
    return (
      <div>
        <h1>Inbox!</h1>
      </div>
    );
  }
});

module.exports = Inbox;
